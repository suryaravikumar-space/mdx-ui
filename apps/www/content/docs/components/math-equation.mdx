---
title: Math Equation
description: Numbered display equations and systems of equations — compose with math-primitives
component: math-equation
published: true
---

## Installation

```bash
pnpm dlx @ravikumarsurya/mdx-ui add math-equation
```

## Equation

A centered display block with an optional equation number. Fill it with [math-primitives](/docs/components/math-primitives) for the equation content.

```mdx
<Equation label="1">
  E = mc
  <Squared />
</Equation>
```

<Equation label="1">
  E = mc
  <Squared />
</Equation>

### Quadratic formula

```mdx
<Equation label="2">
  x ={" "}
  <Frac
    num={
      <span>
        −b <PlusMinus />{" "}
        <Sqrt>
          <Squared>b</Squared> − 4ac
        </Sqrt>
      </span>
    }
    den="2a"
  />
</Equation>
```

<Equation label="2">
  x ={" "}
  <Frac
    num={
      <span>
        −b <PlusMinus />{" "}
        <Sqrt>
          <Squared>b</Squared> − 4ac
        </Sqrt>
      </span>
    }
    den="2a"
  />
</Equation>

### No label

```mdx
<Equation>
  <Nabla />
  <Squared /> <Phi /> = 0
</Equation>
```

<Equation>
  <Nabla />
  <Squared /> <Phi /> = 0
</Equation>

### Inline mode

Use `inline` to wrap an expression inside prose:

```mdx
The identity <Equation inline><Pow exp={<span>i<PiSym /></span>}>e</Pow> + 1 = 0</Equation> connects five constants.
```

The identity <Equation inline><Pow exp={<span>i<PiSym /></span>}>e</Pow> + 1 = 0</Equation> connects five fundamental constants.

## EqSystem

A system of simultaneous equations with an SVG left brace.

```mdx
<EqSystem>
  <div>x + y = 5</div>
  <div>2x − y = 1</div>
</EqSystem>
```

<EqSystem>
  <div>x + y = 5</div>
  <div>2x − y = 1</div>
</EqSystem>

### With primitives

```mdx
<EqSystem>
  <div>
    <PDeriv of="u" by="t" /> = <Alpha /> <Nabla />
    <Squared />u
  </div>
  <div>u(x, 0) = f(x)</div>
  <div>u(0, t) = u(L, t) = 0</div>
</EqSystem>
```

<EqSystem>
  <div>
    <PDeriv of="u" by="t" /> = <Alpha /> <Nabla />
    <Squared />u
  </div>
  <div>u(x, 0) = f(x)</div>
  <div>u(0, t) = u(L, t) = 0</div>
</EqSystem>

## Props

### Equation

| Prop        | Type        | Default | Description                                  |
| ----------- | ----------- | ------- | -------------------------------------------- |
| `label`     | `string`    | —       | Equation number shown right-aligned as `(1)` |
| `inline`    | `boolean`   | `false` | Inline span with no outer box                |
| `children`  | `ReactNode` | —       | Math content — use math-primitives           |
| `className` | `string`    | —       | Additional CSS classes                       |

### EqSystem

| Prop        | Type        | Default | Description              |
| ----------- | ----------- | ------- | ------------------------ |
| `children`  | `ReactNode` | —       | One element per equation |
| `className` | `string`    | —       | Additional CSS classes   |
