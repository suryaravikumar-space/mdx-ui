---
title: Installation
description: Get started with MDX UI in your project in three steps
published: true
---

## Requirements

- Node.js 18 or later
- A React project using Next.js or Vite React
- Tailwind CSS configured

## Quick Start

### 1. Initialize

Run the CLI in your project root:

```bash
npx docsui-cli@latest init
```

This creates `docsui.json` with your project configuration and installs the `cn()` utility.

### 2. Add Components

Add the components you need:

```bash
npx docsui-cli@latest add callout accordion code-block
```

Or run without arguments to pick interactively:

```bash
npx docsui-cli@latest add
```

### 3. Register Components

Add the installed components to your `mdx-components.tsx`:

```tsx
import { Callout } from "@/components/mdx/callout";
import {
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
} from "@/components/mdx/accordion";

export const components = {
  Callout,
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
};
```

## Framework Guides

- [Next.js + @next/mdx](/docs/integration/nextjs-mdx)
- [Next.js + Contentlayer](/docs/integration/nextjs-contentlayer)
- [Next.js + MDX Remote](/docs/integration/nextjs-mdx-remote)

## CLI Reference

| Command                      | Description                           |
| ---------------------------- | ------------------------------------- |
| `docsui-cli init`                | Set up DocsUI in your project         |
| `docsui add [components]`    | Add components                        |
| `docsui update [components]` | Update to latest                      |
| `docsui remove [components]` | Remove components                     |
| `docsui-cli list`                | List all available components         |
| `docsui doctor`              | Check project health                  |
| `docsui new [title]`         | Scaffold a new .mdx page              |
| `docsui save`                | Save MDX content to a structured path |
